"""Agents package containing all agent and workflow modules.

In ADK development, this root directory serves as the discovery root when running:
    `adk web ./agents`
or
    `adk run ./agents/<workflow_package>`
"""
