This agent takes a user query for a website and generates a single self-contained HTML file with embedded CSS and JavaScript. 
It then saves the file using a file writer tool that names the file using the current timestamp.